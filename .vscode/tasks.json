{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Build C Program",
            "command": "gcc",
            "args": [
                "-g",
                "-mconsole",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}.exe"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Compile active C file into an exe"
        }
    ]
}
